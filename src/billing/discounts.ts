export function calculateDiscount(coupon: string): number {
  if (coupon === "WELCOME10") {
    return 10;
  }

  return 0;
}

// agent added WELCOME20 discount support

// agent added WELCOME20 discount support

// agent added WELCOME20 discount support
